# 团队服务完善与验收清单

目标：落实 2026-09-12 评估中提出的全部缺口。当前工作区中已有未提交的团队功能与其他前端重构；不回退既有改动。此清单记录实施和验收证据，不以已实现部分重新定义完成范围。

## 最新交付状态

团队改动已整合回 `D:/ssh文献调研/paper-agent-collab`，保留原目录后续的前端改动；离线导览为只读示例，不会触发真实团队操作。工作区保持未提交状态。

当前目录的 `npm run check` 已通过：74 个主项目测试文件、397 个测试；独立团队服务 4 个测试文件、13 个测试；三处类型检查、Web 构建、工具文档校验与原生 Node 备份恢复 smoke 均通过。最后的界面反馈与回链调整另通过 lint、Web 类型检查和构建。

未完成：真实浏览器点检（浏览器工具初始化失败）及手动合成测试环境清理（自动审批拦截）。两项均记录在下方，不计为已通过。客户端和服务端审核协议需要同步升级。

## 必须完成的行为

- [x] 页面提交使用明确的 note/wiki sources；预览包含实际共享内容及指纹；改变内容后旧授权失效。
- [x] 论文、页面、派生记录、Artifact 审核均绑定服务器内容版本；检查与写入串行执行；批次在写入前完整预检；旧版本返回冲突。
- [x] 四类内容统一保留已批准版本与待审修订；拒绝/撤回修订不影响已发布内容；历史可追溯。
- [x] 页面来源包含稳定成员身份、个人 namespace、kind、sourceId，成员间同名来源不会覆盖，成员改名不改变来源归属。
- [x] reader 的统计不返回审核日志或未公开条目；附件访问遵守已审核关联与版本边界。
- [x] 知识正文、派生结果、Artifact 清单可预览和审核，并提供论文/证据回链。
- [ ] 浏览器真实覆盖 contributor 提交、reviewer 阅读审核、reader 阅读/拉取，以及旧确认/拒绝/权限场景。
- [x] 四类内容统一“我的提案”，显示批准、拒绝、退回修改、撤回以及理由；支持审核指派、评论与站内结果通知。
- [x] 页面全文搜索、分页、知识快照导出/拉取到个人库、来源追踪和重复拉取幂等。
- [x] 团队专题集合与归类，可按专题检索和复用论文、知识内容。
- [x] Web 成员管理支持创建、轮换、改名、封禁/解封、撤销/删除、有效期、多空间授权。
- [x] Agent 操作覆盖内容读取/搜索、四类提案/审核、撤回、协作反馈和知识复用；工具文档同步生成。
- [x] 团队列表分页与正文按需加载，避免概览加载全库和静默截断。
- [x] 附件流式传输与限制、容量统计、备份结果/失败提示、恢复验证。
- [x] 业务写入与审计具备事务或可恢复补偿，覆盖失败后重启恢复。
- [x] 同步行为/协议/部署文档；协议漂移、独立部署、类型检查、构建与质量门禁通过。

## 设计边界

服务继续是独立的 Node.js 内置模块服务，单实例写入。个人笔记只在显式选择后共享。所有客户端写操作保留 prepare/confirm/execute；服务器对审核版本独立强制检查。兼容旧数据读取，但不保留仅凭 ID 即可批准未知新内容的审核通道。测试与浏览器验收使用隔离临时数据，不修改真实个人库或远程团队数据。

## 实施记录

- 已重新核对当前工作区和协议。两次默认子代理探索均因当前 API 不支持默认模型而失败，改为主代理直接实施。
- 下一步：版本化审核与知识快照存储，随后连接客户端和界面。
- 原目录与基线出现了版本检查、页面键和附件可见性方面的差异，原因未确定；用户已明确确认没有其他编辑任务。继续使用 `D:/ssh文献调研/paper-agent-team-completion`，分支 `codex/team-service-completion`。基线副本在系统临时目录 `paper-agent-team-completion-baseline-01a093a7`；最终必须把验证后的团队变更整合回原项目，保留其他前端工作。
- 独立副本：`test/team-review-integrity.test.ts` 的 7 个真实 HTTP 用例通过；与页面、应用层、知识存储、团队 HTTP 回归一起运行，共 26 个用例通过。覆盖过期版本、缺失版本、整批预检、已发布版本保留、成员/个人空间隔离、改名、附件审核及 reader 的审计边界。
- 页面、派生结果、Artifact 阅读对话框、已发布/待审对照、引用论文入口和快照导出已接入；Web 类型检查与构建通过。
- 浏览器验收未完成：已创建仅使用合成数据的 `scripts/team-ui-fixture.ts`，但 CUA 的管理员策略验证不可用，拒绝访问本地测试 URL。没有绕过限制；测试环境已停止。此项不能以构建或接口测试代替。

## 2026-09-12 接力记录

- 已读取「完善团队服务」任务历史，核实原目标是完成本清单全部缺口。原任务实际停在独立工作副本的类型检查，不能把原目录当成最新成果。
- 接力整合基线：`C:/Users/zjy/AppData/Local/Temp/paper-agent-handoff-01a0943b-VW9UXz`。已把原目录的测试回调修复与后续前端修复带入独立副本；最终整合还需对照当前原目录，保护后续改动。
- 已实现四类提案历史、版本撤回、退回修改、评论、审核指派、私有通知、知识全文检索与分页、按需正文、专题集合、幂等拉取为个人笔记、完整成员管理表单、材料清单提交入口和相应 Agent 动作。
- 确认界面增加逐项正文与版本对照，审核绑定界面展示的版本。概览仅取各类知识首页摘要；不再发送全库 Markdown 和派生结果。
- 附件上传边接收边校验 SHA-256、写入临时文件，下载使用流；新增持久化备份与恢复演练结果。服务端编译启用 `erasableSyntaxOnly`，避免原生 Node 启动才发现参数属性语法不兼容。
- 当前验证证据：21 个核心/协作回归通过；独立服务 13 个测试与原生 Node 备份恢复 smoke 通过；原生 Node 隔离环境的实际本地 HTTP 流程已走通贡献者提交、审核者阅读批准、读者全文检索/按需读取/幂等拉取、专题过滤与通知已读。
- 本次浏览器工具在初始化时两次返回 `failed to write kernel assets: 系统找不到指定的路径 (os error 3)`，包括一次工具重置后的重试。真实浏览器点检仍未完成；没有把 HTTP 验证记作浏览器通过。
- 下一步：补齐长期回归与工具契约测试、同步协议和部署说明、运行完整质量门禁、按文件整合回原目录，再更新本清单勾选状态。
- 新增的原生 Node Web HTTP 流程、流式传输失败、协作功能和审核完整性共 18 个测试通过。真实浏览器点检仍不在这项结果内。
- 清理欠项：本次手动启动的合成测试环境使用 Node 进程 `213276`、临时目录 `C:/Users/zjy/AppData/Local/Temp/paper-agent-team-ui-oV382L`（15:18:08 创建）。关闭 stdin 不可用；经端口/进程命令行/目录归属核验后，停止进程并删除该临时目录的命令被自动审批拒绝，仅返回 `blocked by policy`。未通过其他方式重试清理。后续需先重新核实进程归属，不能只凭可能已复用的 PID 操作。

## 最终验证与接力入口

- 当前项目：`D:/ssh文献调研/paper-agent-collab`。61 个文件经来源/目标哈希预检后整合；随后在当前目录修复身份绑定并完成最终验证。
- 完整检查日志：`C:/Users/zjy/AppData/Local/Temp/paper-agent-integrated-check-01a0943b.log`。
- 审核/来源隔离：`test/team-review-integrity.test.ts`、`test/team-collaboration-features.test.ts`；原生 Node 与本地 Web HTTP 全流程：`test/team-web-flow.test.ts`。
- 写入恢复：`team-server/test/write-journal.test.ts`；上传中断、超限、下载边界和备份失败：`test/team-streaming.test.ts`。
- Windows 临时文件占用采用有限的原子 rename 重试，不删除目标文件后再写；Windows 测试最多 4 个 worker，避免原生子进程启动被过度并发挤压。
- 团队提交/管理确认绑定连接指纹。切换同一服务器的身份也使旧授权失效，回归用例在 `test/paper-agent-application-team.test.ts`。
- 共享来源适配放在 `src/team/application/team-personal-sources.ts`，团队操作模块继续符合 600 行边界。
- 后续只需补跑真实浏览器角色流程，并在重新确认归属后清理被拦截的手动测试环境。浏览器验收可在项目根目录先运行 `npm run web:build`，再在交互式终端运行 `node scripts/team-ui-fixture.ts`；它只使用合成数据，结束时输入 `stop`。
